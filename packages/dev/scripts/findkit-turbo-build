#!/bin/sh

# Kinda like https://github.com/vercel/turborepo/issues/550

set -eu


package_name=$(node -p "require('./package.json').name")

cd ../..

exec node_modules/.bin/turbo run build --scope=$package_name --include-dependencies --no-deps $@